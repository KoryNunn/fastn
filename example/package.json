{
  "name": "fastn-example",
  "version": "1.0.0",
  "description": "Example app for fastn",
  "main": "index.js",
  "scripts": {
    "watch": "watchify index.js -o index.browser.js -d & stylus -u nib -w style.styl -o style.css",
    "build": "browserify -g uglifyify index.js --noparse clone > index.browser.js & stylus -u nib style.styl -o style.css"
  },
  "author": "Kory Nunn",
  "license": "ISC",
  "dependencies": {
    "cpjax": "^1.0.0",
    "crel": "^2.1.5",
    "debounce": "^1.0.0",
    "doc-js": "^1.2.0",
    "fastn": "^2.10.0",
    "flat-merge": "^1.0.0",
    "laidout": "^1.0.0"
  },
  "devDependencies": {
    "uglifyify": "^5.0.2"
  }
}
