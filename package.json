{
  "name": "fastn-example",
  "version": "1.0.0",
  "description": "Home app for fastn",
  "main": "index.js",
  "scripts": {
    "watch": "watchify index.js -o index.browser.js -d & stylus -u nib -w style.styl -o style.css",
    "build": "browserify -g uglifyify index.js --noparse clone > index.browser.js & stylus -u nib style.styl -o style.css",
    "buildAll": "npm run build; cd example; npm run build; cd ../try; npm run build",
    "watchTry": "cd ../try; npm run watch"
  },
  "author": "Kory Nunn",
  "license": "ISC",
  "dependencies": {
    "cpjax": "^1.0.0",
    "crel": "^2.1.5",
    "debounce": "^1.0.0",
    "doc-js": "^1.2.0",
    "fastn": "^2.11.0",
    "flat-merge": "^1.0.0",
    "highlight": "^0.2.3",
    "laidout": "^1.0.0",
    "lzutf8": "^0.3.2"
  },
  "devDependencies": {
    "browserify": "^14.5.0",
    "nib": "^1.1.0",
    "stylus": "^0.51.1",
    "uglifyify": "^3.0.1",
    "watchify": "^3.11.1"
  }
}
